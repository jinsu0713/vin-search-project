rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 🌟 모든 VIN 검색 웹 요청에 대해 'cars' 컬렉션의 읽기(조회)만 영구적으로 허용합니다.
    match /cars/{carId} { 
      allow read: if true;
      allow write: if false; 
    }
    
    // 다른 모든 문서에 대한 접근은 명시적으로 차단합니다.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}